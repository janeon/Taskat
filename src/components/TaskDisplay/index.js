import React, { Component } from 'react';
import View from './View';

class TaskDisplay extends Component {
    constructor(props) {
        super(props);

        // we are going to pass things in through here (like all the tabs, updating task, etc.)
    }

    render() {
        return <View />;
    }

}

export default TaskDisplay;